{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "LP",
      "runtimeExecutable": "/usr/bin/ruby",
      "runtimeArgs": ["-e", "require 'webrick'; s=WEBrick::HTTPServer.new(Port:8080,DocumentRoot:'/tmp/tj_lp'); trap('INT'){s.shutdown}; s.start"],
      "port": 8080
    }
  ]
}
