# Credentials — never commit
*credentials*.json
techno-japan-*-*.json
scripts/techno-japan-*.json

# Logs
*.log
scripts/*.log

# OS noise
.DS_Store

# Node / data pipeline generated artifacts
node_modules/
validation-report.txt

# macOS/クラウド同期が作る重複コピー（"foo 2.html" / "foo 3.html"）
* [0-9].html
* [0-9].*

# 移行作業用のシート書き出し置き場。
# ⚠️ このリポジトリは PUBLIC。ここに置くデータ（海外フェス700件等の
# 内部データベース）は絶対にコミットしないこと。git add -A でも
# 拾われないよう、フォルダごと除外する（2026-08-19 / DB移行 Step 2）。
data/migration/
